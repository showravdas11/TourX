const MyOrder = ({ order }) => {
    // const { name, time, price, descliption, img } = order;
    return (
        <div>
            {/* <h2>{name}</h2> */}
        </div>
    );
};

export default MyOrder;